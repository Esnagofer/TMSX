package esnagofer.msx.ide.emulator.core.domain.model.emulator.debugger;

/**
 * The Enum StepExecute.
 */
public enum DebuggerStepExecute {
	
	/** The step over. */
	STEP_OVER,
	
	/** The step out. */
	STEP_OUT,
	
	/** The step into. */
	STEP_INTO,
	
	/** The run. */
	RUN;

}
