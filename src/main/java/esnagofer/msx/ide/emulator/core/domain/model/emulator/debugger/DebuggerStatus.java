package esnagofer.msx.ide.emulator.core.domain.model.emulator.debugger;

/**
 * The Enum DebuggerStatus.
 */
public enum DebuggerStatus {
	
	/** The ds inactive. */
	DS_INACTIVE,
	
	/** The ds waiting. */
	DS_WAITING,
	
	/** The ds running. */
	DS_RUNNING,
	
	/** The ds exec for step over. */
	DS_EXEC_FOR_STEP_OVER,
	
	/** The ds exec for step out. */
	DS_EXEC_FOR_STEP_OUT,
	
	/** The ds exec for step into. */
	DS_EXEC_FOR_STEP_INTO

}
